# Required on the Vercel project (server-side, never exposed to the client)
GEMINI_API_KEY=

# Only required for native (Android APK) builds — the absolute URL of the deployed backend,
# e.g. https://agon-preview.vercel.app . Leave unset for web, where /api is same-origin.
EXPO_PUBLIC_API_BASE_URL=
